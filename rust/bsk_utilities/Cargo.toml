[package]
name = "bsk-utilities"
version = "0.1.0"
edition = "2021"
description = "Rust bindings and safe wrappers for Basilisk C ABI utilities"
license = "ISC"

# `src/raw.rs` is generated and committed by
# `tools/gen_rust_utilities.py` from the vendored Basilisk headers.

[dependencies]
