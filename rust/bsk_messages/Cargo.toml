[package]
name = "bsk-messages"
version = "0.1.0"
edition = "2021"
description = "Auto-generated Rust FFI bindings for Basilisk C message types"
license = "ISC"

# This crate contains no build-time code; its src/lib.rs is pre-generated by
# tools/gen_rust_messages.py from the vendored bsk_sdk headers and committed.
# Re-run that script after every `sync_headers.py` update.

[dependencies]
