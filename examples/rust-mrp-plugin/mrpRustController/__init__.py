"""mrpRustController — BSK attitude controller implemented in Rust.

The SWIG-generated Python wrapper (``mrpRustController.py``) and the
compiled extension (``_mrpRustController.so``) live in this directory
alongside the Rust source.  Both are build artifacts; do not commit them.
"""
